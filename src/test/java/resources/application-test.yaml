server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:casa_do_codigo_api_db_test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect